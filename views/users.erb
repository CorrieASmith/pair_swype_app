<h3 class="header center">Students</h3>
<div class="col s10 m9 l10">

  <table class="hoverable centered">
    <thead>
      <tr>
          <th data-field="name">Name</th>
          <th data-field="language">Cohort</th>
      </tr>
    </thead>

    <tbody>
      <% @users.each do |user| %>
      <tr>
        <td><a href="/users/<%= user.id %>"><%= user.name %>  <%= user.last_name %></a></td>
        <td><a href="/cohorts/<%= user.cohort.id %>"><%= user.cohort.language %> <%= user.cohort.season %> <%= user.cohort.year %></a></td>
      </tr>
      <% end %>
    </tbody>
  </table>
</div>
